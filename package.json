{
  "name": "dynamodb-data-mapper-js",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "docs": "typedoc src && lerna run docs",
    "pretest": "lerna run pretest",
    "test": "jest",
    "deploy": "npx lerna publish from-package --yes --no-verify-access"
  },
  "author": {
    "name": "AWS SDK for JavaScript Team",
    "email": "aws-sdk-js@amazon.com"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^8.0.4",
    "jest": "^29.7.0",
    "lerna": "^3.13",
    "typedoc": "^0.14.0",
    "typescript": "^6.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": ["/node_modules/", "\\.ts$"]
  }
}
